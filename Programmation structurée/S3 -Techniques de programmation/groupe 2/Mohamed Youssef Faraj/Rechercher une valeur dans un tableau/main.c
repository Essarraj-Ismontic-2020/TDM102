#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
int rechche (int*T, int N, int V){
	int R=-1,i;
	for (i=0 ; i<N ; i++){
		if (T[i]==V){
			R=i;
		}
	}
}
	return 0;
}

