#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
		char T[7]={'C','H','A','I','M','A','E'}, a=0;
	T[7]=a;
	for(a=0; a<7 ; a++){
		printf("%c", T[a]);
	
	}
	return 0;
}
