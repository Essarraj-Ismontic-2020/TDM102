#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
  void carre (float y , float i ){
  
   float  carre ;
    carre =y*i; 
    printf(" carre est : %f",carre);
	}
 
int main(int argc, char *argv[]) {
	 carre(2.2,2.1);
	
	 
	return 0;
}
