#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int a=1;
	int b=1;
	while(a<=3) {
		printf("Apprenant numero %d \n",a);
		a++;

	} 
	while(b<=2) {
		printf("rapporteur numero %d \n",b);
		b++;
	} 
	return 0;
}
