#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
int somme(a,b){
	int u;
	u=a+b;
	return u;
}

int main(int argc, char *argv[]) {
	printf("est %d,somme(a,b)");
	return 0;
}
