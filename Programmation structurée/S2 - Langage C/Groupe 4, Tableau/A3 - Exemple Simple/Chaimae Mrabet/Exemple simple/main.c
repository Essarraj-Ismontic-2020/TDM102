#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {

    int tableau[4] = {10, 20, 30, },a=0, b=1, c=2;
    
   
    {
        printf("%d\n", tableau[a]);
        printf("%d\n",tableau[b]);
        printf("%d",tableau[c]);
    }



	return 0;
}
