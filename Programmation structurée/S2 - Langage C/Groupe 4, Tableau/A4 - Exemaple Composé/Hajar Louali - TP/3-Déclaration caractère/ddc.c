#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	char T[5]={'H','A','J','A','R'}, m=0;
	T[5]=m;
	for(m=0; m<5 ; m++){
		printf("%c", T[m]);
	
	}
	
	return 0;
}
