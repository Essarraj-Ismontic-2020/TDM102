#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
//CHIKHIYA ILHAM
int main(int argc, char *argv[]) {
	char i, T[5]={105,108,104,97,109};
	T[5]=i;
	for (i=0,i<6,i++)
	{
	printf("%c",T[i]);
	}
	
	return 0;
}
