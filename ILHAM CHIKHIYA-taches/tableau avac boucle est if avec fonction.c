#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
void show(){
		int tab[100] = {50,60,70,80,90};
	int i = 0;
		for(i=0;i<5;i++){
		printf("le contenu du tableau %d \n",tab[i]);
		if(tab[i] == 90){
			printf("le tableau est fini\n");
			
			
		}
}
}
int main(int argc, char *argv[]) {

show();
	
	return 0;
}
